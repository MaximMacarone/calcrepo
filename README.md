# SimpleCalculator - iOS App

Простое iOS приложение-калькулятор, демонстрирующее настройку CI/CD с GitHub Actions.

## 📱 О проекте

SimpleCalculator - это современное iOS приложение, построенное с использованием:
- **Swift 6.1+** и **SwiftUI**
- **Swift Package Manager** для модульной архитектуры
- **Swift Testing** для unit-тестов
- **GitHub Actions** для непрерывной интеграции и развертывания

## 🏗️ Архитектура проекта

Проект использует архитектуру **workspace + SPM package**:

```
SimpleCalculator/
├── SimpleCalculator.xcworkspace/              # Откройте этот файл в Xcode
├── SimpleCalculator.xcodeproj/                # Оболочка приложения
├── SimpleCalculator/                          # Точка входа приложения
│   ├── Assets.xcassets/                       # Ресурсы приложения
│   ├── SimpleCalculatorApp.swift              # @main точка входа
│   └── SimpleCalculator.xctestplan            # Конфигурация тестов
├── SimpleCalculatorPackage/                   # 🚀 Основная область разработки
│   ├── Package.swift                          # Конфигурация пакета
│   ├── Sources/SimpleCalculatorFeature/       # Код функций
│   │   ├── Calculator.swift                   # Модель калькулятора
│   │   └── ContentView.swift                  # UI калькулятора
│   └── Tests/SimpleCalculatorFeatureTests/    # Unit-тесты
│       └── SimpleCalculatorFeatureTests.swift
└── SimpleCalculatorUITests/                   # UI автоматизированные тесты
```

## 🚀 Быстрый старт

### Требования
- macOS 14.0+
- Xcode 16.0+
- Swift 6.1+

### Установка

1. Клонируйте репозиторий:
```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd pz2
```

2. Откройте workspace в Xcode:
```bash
open SimpleCalculator.xcworkspace
```

3. Выберите симулятор и запустите проект (⌘R)

### Запуск тестов

```bash
# Через Swift Package Manager
cd SimpleCalculatorPackage
swift test

# Или через Xcode
# Product > Test (⌘U)
```

## 🔧 Функциональность

Калькулятор поддерживает следующие операции:
- ➕ Сложение
- ➖ Вычитание
- ✖️ Умножение
- ➗ Деление
- 🗑️ Очистка

## 🧪 Тестирование

Проект включает unit-тесты, покрывающие:
- Базовые математические операции
- Обработку ошибок (деление на ноль)
- Сброс калькулятора
- Последовательные операции

Запуск тестов:
```bash
cd SimpleCalculatorPackage
swift test
```

## 🔄 CI/CD

Проект настроен с использованием **GitHub Actions** для автоматизации:

### Workflow файлы

1. **`.github/workflows/main.yml`** - Основной CI workflow:
   - Запускается при push в `main`, `development`, `release/**`
   - Запускается при создании Pull Request
   - Запускается при создании тегов версий
   - Выполняет:
     - ✅ Запуск unit-тестов
     - ✅ Сборка приложения
     - ✅ Генерация документации
     - ✅ Создание релизов

2. **`.github/workflows/pr.yml`** - Проверки Pull Request:
   - Валидация кода
   - Запуск тестов
   - Комментирование PR со статусом

### Автоматические проверки

CI автоматически запускается при:
- Каждом коммите в репозиторий
- Создании Pull Request
- Создании тега версии

### Просмотр статуса CI

1. Перейдите на GitHub в ваш репозиторий
2. Откройте вкладку **Actions**
3. Просмотрите историю запусков workflow
4. Откройте конкретный run для просмотра логов

## 📚 Документация

Документация автоматически генерируется при каждом коммите через CI/CD.

### Доступная документация

- **[API Documentation](docs/API.md)** - API справочник по модулям и классам
- **[Architecture](docs/ARCHITECTURE.md)** - Описание архитектуры проекта
- **[Changelog](docs/CHANGELOG.md)** - История изменений проекта

### Генерация документации локально

```bash
xcodebuild docbuild \
  -workspace SimpleCalculator.xcworkspace \
  -scheme SimpleCalculator \
  -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest'
```

## 🌿 Git Flow

Проект использует следующую структуру веток:

```
main (production)
  ├── development (разработка)
  │     └── feature/* (новые функции)
  └── release/* (подготовка релизов)
```

### Работа с ветками

1. **Development** - основная ветка разработки
2. **Feature branches** - ветки для новых функций
3. **Release branches** - ветки для подготовки релизов
4. **Main** - стабильная production ветка

### Создание релиза

```bash
# Создание ветки release
git checkout -b release/v1.0.0

# Обновление версии, исправление багов
# ...

# Создание тега
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

## 📝 План выполнения лабораторной работы

Подробный план выполнения всех шагов лабораторной работы находится в файле [`STEP_BY_STEP.md`](STEP_BY_STEP.md).

## 🎯 Статус проекта

✅ **Версия 1.0.0 выпущена** - Проект готов к использованию

- ✅ CI/CD настроен и работает
- ✅ Все тесты проходят успешно
- ✅ Документация создана и обновляется автоматически
- ✅ Release ветка создана и протестирована
- ✅ Тег v1.0.0 опубликован

См. [Release Notes](docs/RELEASE_NOTES.md) для подробностей о релизе.

## 🛠️ Разработка

### Добавление новых функций

1. Создайте feature ветку:
```bash
git checkout -b feature/new-feature
```

2. Внесите изменения в `SimpleCalculatorPackage/Sources/SimpleCalculatorFeature/`

3. Добавьте тесты в `SimpleCalculatorPackage/Tests/SimpleCalculatorFeatureTests/`

4. Запустите тесты локально:
```bash
cd SimpleCalculatorPackage
swift test
```

5. Создайте Pull Request в `development`

### Структура кода

- **Модели**: `Calculator.swift` - бизнес-логика калькулятора
- **UI**: `ContentView.swift` - SwiftUI интерфейс
- **Тесты**: `SimpleCalculatorFeatureTests.swift` - unit-тесты

## 📦 Зависимости

Проект использует только стандартные библиотеки Apple:
- SwiftUI
- Foundation
- Testing (Swift Testing framework)

## 🔐 Лицензия

Этот проект создан в учебных целях.

## 👥 Авторы

Создано для лабораторной работы по ТРПО.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи CI в GitHub Actions
2. Убедитесь, что все тесты проходят локально
3. Проверьте версии Xcode и Swift

---

**Версия**: 1.0.0  
**Последнее обновление**: 2025-01-22
