# Руководство по CI/CD

Это руководство описывает настройку и использование системы непрерывной интеграции и развертывания для проекта SimpleCalculator.

## 📋 Содержание

1. [Обзор CI/CD](#обзор-cicd)
2. [Настройка GitHub Actions](#настройка-github-actions)
3. [Workflow файлы](#workflow-файлы)
4. [Автоматические проверки](#автоматические-проверки)
5. [Работа с ветками](#работа-с-ветками)
6. [Создание релизов](#создание-релизов)
7. [Устранение неполадок](#устранение-неполадок)

## 🔄 Обзор CI/CD

Проект использует **GitHub Actions** для автоматизации следующих процессов:

- ✅ **Автоматическое тестирование** - запуск unit-тестов при каждом коммите
- ✅ **Автоматическая сборка** - проверка компиляции приложения
- ✅ **Проверка Pull Request** - валидация изменений перед слиянием
- ✅ **Генерация документации** - автоматическое создание документации
- ✅ **Создание релизов** - автоматизация процесса релиза

## ⚙️ Настройка GitHub Actions

### Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Создайте новый репозиторий
3. Не инициализируйте его с README, .gitignore или лицензией

### Шаг 2: Подключение локального репозитория

```bash
# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: iOS Calculator with CI/CD"

# Подключение к GitHub
git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
git branch -M main
git push -u origin main
```

### Шаг 3: Проверка работы CI

1. После push перейдите на GitHub
2. Откройте вкладку **Actions**
3. Убедитесь, что workflow запустился автоматически
4. Проверьте статус выполнения

## 📁 Workflow файлы

### 1. `.github/workflows/main.yml`

Основной CI workflow, который запускается при:
- Push в ветки: `main`, `development`, `release/**`
- Создании Pull Request
- Создании тегов версий (например, `v1.0.0`)

**Выполняемые задачи:**
- `test` - Запуск unit-тестов через Swift Package Manager
- `build` - Сборка iOS приложения для симулятора
- `documentation` - Генерация документации (только для main/development)
- `release` - Создание GitHub Release при создании тега

### 2. `.github/workflows/pr.yml`

Workflow для проверки Pull Request:
- Запуск тестов
- Сборка приложения
- Проверка форматирования кода
- Комментирование PR со статусом

## ✅ Автоматические проверки

### При каждом коммите

CI автоматически:
1. Запускает все unit-тесты
2. Собирает приложение для проверки компиляции
3. Генерирует документацию (для main/development)

### При создании Pull Request

Дополнительно:
1. Валидирует изменения
2. Проверяет, что все тесты проходят
3. Оставляет комментарий в PR со статусом

### При создании тега версии

1. Запускает все проверки
2. Создает GitHub Release
3. Публикует артефакты сборки

## 🌿 Работа с ветками

### Создание ветки development

```bash
git checkout -b development
git push -u origin development
```

### Разработка новой функции

```bash
# Переключение на development
git checkout development

# Создание feature ветки (опционально)
git checkout -b feature/new-feature

# Внесение изменений
# ...

# Коммит и push
git add .
git commit -m "Add new feature"
git push

# CI автоматически запустится для этой ветки
```

### Создание Pull Request

1. Перейдите на GitHub
2. Нажмите **Compare & pull request**
3. Заполните описание
4. Создайте PR из `development` в `main`
5. CI автоматически проверит PR
6. После успешного прохождения тестов, слейте PR

## 🚀 Создание релизов

### Шаг 1: Создание ветки release

```bash
git checkout -b release/v1.0.0
git push -u origin release/v1.0.0
```

### Шаг 2: Подготовка к релизу

1. Обновите версию в проекте:
   - Xcode: Project Settings > General > Version
   - README.md: обновите версию в документации
   - CHANGELOG.md: добавьте записи о изменениях

2. Исправьте баги, если есть

3. Закоммитьте изменения:
```bash
git add .
git commit -m "Prepare release v1.0.0"
git push
```

### Шаг 3: Проверка CI

Убедитесь, что CI успешно прошел для ветки release.

### Шаг 4: Создание тега

```bash
# Создание аннотированного тега
git tag -a v1.0.0 -m "Release version 1.0.0"

# Push тега
git push origin v1.0.0
```

**Или через GitHub:**
1. Перейдите в **Releases** на GitHub
2. Нажмите **Create a new release**
3. Выберите тег `v1.0.0` (создастся автоматически)
4. Заполните описание релиза
5. Опубликуйте release

### Шаг 5: Слияние release

После создания тега:
1. Создайте PR из `release/v1.0.0` в `main`
2. После успешного прохождения тестов, слейте PR
3. Также слейте изменения в `development` (если нужно)

## 🔍 Просмотр результатов CI

### На GitHub

1. Перейдите в репозиторий
2. Откройте вкладку **Actions**
3. Выберите нужный workflow run
4. Просмотрите логи каждого шага

### Статус коммита

На странице коммита вы увидите:
- ✅ Все проверки прошли
- ❌ Одна или несколько проверок не прошли
- ⏳ Проверки выполняются

### В Pull Request

В PR отображается:
- Статус всех проверок
- Комментарии от CI бота
- Возможность слияния только после успешных проверок

## 🐛 Устранение неполадок

### CI не запускается

**Проблема:** Workflow не запускается после push

**Решение:**
1. Проверьте, что файлы workflow находятся в `.github/workflows/`
2. Убедитесь, что синтаксис YAML корректен
3. Проверьте, что ветка указана в `on.push.branches`

### Тесты не проходят

**Проблема:** Тесты падают в CI, но проходят локально

**Решение:**
1. Проверьте версию Xcode в CI (может отличаться от локальной)
2. Убедитесь, что все зависимости установлены
3. Проверьте логи CI для деталей ошибки

### Сборка не проходит

**Проблема:** Приложение не компилируется в CI

**Решение:**
1. Проверьте, что все файлы добавлены в Git
2. Убедитесь, что версия Xcode совместима
3. Проверьте настройки проекта (deployment target, etc.)

### Документация не генерируется

**Проблема:** Документация не создается

**Решение:**
1. Убедитесь, что вы в ветке main или development
2. Проверьте логи шага `documentation`
3. Документация генерируется только для определенных веток

## 📊 Мониторинг CI

### Метрики

Отслеживайте:
- Время выполнения workflow
- Процент успешных сборок
- Количество падающих тестов

### Уведомления

Настройте уведомления GitHub для:
- Неудачных сборок
- Новых Pull Request
- Созданных релизов

## 🔐 Секреты и переменные окружения

Если понадобятся секреты (API ключи, токены):

1. Перейдите в Settings > Secrets and variables > Actions
2. Добавьте новый секрет
3. Используйте в workflow:
```yaml
env:
  API_KEY: ${{ secrets.API_KEY }}
```

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Swift Package Manager](https://swift.org/package-manager/)
- [Swift Testing](https://www.swift.org/documentation/testing/)

## ✅ Чеклист настройки CI/CD

- [ ] Репозиторий создан на GitHub
- [ ] Локальный репозиторий подключен к GitHub
- [ ] Workflow файлы добавлены в `.github/workflows/`
- [ ] Первый коммит сделан и push выполнен
- [ ] CI запустился автоматически
- [ ] Все тесты проходят успешно
- [ ] Сборка приложения проходит успешно
- [ ] Pull Request проверяется автоматически
- [ ] Релизы создаются автоматически при создании тега

---

**Примечание:** Этот CI/CD настроен для учебных целей. Для production проектов может потребоваться дополнительная настройка безопасности и оптимизация.

